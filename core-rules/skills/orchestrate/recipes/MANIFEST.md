# Recipe manifest

One row per recipe. Recipes are generic, parametric skeletons — specifics enter through `args`, never as baked literals. The capability and degrade columns echo the two-level graceful degrade in `SKILL.md`. The **Loop safety** column records each recipe's `meta.safety` declaration — every recipe is a loop and must honor the loop-safety contract (`core-rules/loop-safety.md`); **a recipe with no `safety` declaration is non-compliant** (flagged by `cross-project-process-audit`, a `process-gate` / review finding).

| Recipe | Intent | Inputs (`args`) | Capability needs | Loop safety (`meta.safety`) | Degrade note |
|---|---|---|---|---|---|
| `fanout-verify` | Fan out one verified-change agent per target, push + PR each, return verdicts for the main loop to merge/hold. | `targets[]` (`{name,path}`; falls back to the registry), `task`, `branchPrefix`. | Workflow tool ideal; subagents + isolated worktrees required for parallel fan-out. | One-shot fan-out → `no_progress_iterations: null` (no rounds to measure); `max_iterations` + `budget_ceiling_usd` omitted to inherit the resolved baseline; signal commit/PR. | No workflow tool → dispatch the per-target stage sequentially per this row + `SKILL.md`. No subagents → run targets inline, one at a time, preserving change → verify → verdict. |
| `codex-executor` | Mixed-harness fan-out: route execution-heavy bounded units through the blocking `codex-worker`, keep planning/review/synthesis on the orchestrator, and independently review the actual diff in the producer's same worktree. Contract + dispatch mechanics: [`references/codex-executor.md`](../references/codex-executor.md), intent: `docs/codex-routing.md`. | `units[]` (`{name,kind,task,effort,targetCwd}` + optional `justification`/`paths`/`constraints`/`nonGoals`/`proof`; **`targetCwd` required** on execute units and caller-provisioned; **`effort` required** on execute units, enum `xhigh\|max` (medium/high suspended 2026-07-10 per `docs/codex-routing.md §3`); omitted = validation error, `ultra` = hard reject, `max` requires non-empty `justification`), `supportedEfforts`, `codexAvailable`, `branchPrefix`. | Workflow tool + inherited `codex-worker`; Codex is runtime-detected, not a dependency. Producer and verifier share the explicit target worktree; per-agent engine isolation is not used because it would sever the diff. | One-shot fan-out → `no_progress_iterations: null`; ceilings inherit; signal commit/PR. Cross-harness rates come from config. | No Codex, unsupported surface tier, UNAVAILABLE / FAILURE, empty/error result, or defensive handle assertion → identical unit runs on Claude in the same `targetCwd`, logged and never clamped. The caller commits only after verification. No workflow/subagents → preserve route → execute → same-worktree actual-diff review sequentially. |
| `codex-fanout` | Mixed Codex/Claude implementation fan-out: blocking generation → cheap Claude actual-diff/proof verification → at most one same-leg fix + fresh reverify; returns merge-ready receipts in dependency order and never commits or merges. | `units[]` (`{name,leg:'codex'\|'claude',task,paths,proofCmd,conflicts?,targetCwd?,dependsOn?}` plus **required Codex `effort`** enum `xhigh\|max` (medium/high suspended 2026-07-10 per `docs/codex-routing.md §3`) and optional `justification`; `conflicts:true` requires a caller-provisioned per-unit `targetCwd` shared across generate/verify/fix), `codexCap` (required caller-resolved positive integer from `trellis.config.json.codex_fanout.concurrency`, central example 4; never baked into recipe logic), `codexAvailable`, optional run-level `targetCwd` / `companionPath`. | Workflow tool + subagents + inherited `codex-worker`; Claude units consume no Codex slot. Conflicting units use one explicit stable target worktree; per-stage engine isolation is forbidden because it loses the produced diff. | `max_iterations: null` and `budget_ceiling_usd: null` inherit; `no_progress_iterations: 2` lets the first worker stall retry and halts on the second consecutive no-progress event; signal file delta. | Codex unavailable / UNAVAILABLE / FAILURE / empty/error → identical unit runs on Claude with `claude(degraded)` receipt. No workflow/subagents → reproduce bounded waves sequentially, preserve independent verification, one-fix cap, and dependency receipt order. Orchestrator alone commits/merges. |
| `verify-panel` | Cross-model diversity verification: per hard finding, run a Claude reviewer + a Codex reviewer in parallel, merge into a consensus (`agree-real` / `agree-not-real` / `split` / `single-model`), degrade to Claude-only when Codex is absent. Contract: [`references/verify-panel.md`](../references/verify-panel.md), intent: `docs/codex-routing.md` §2. | `findings[]` (`{id,claim,file,line,severity}`), `context` (evidence), **`targetCwd` required** for the Codex work order, **`effort` required** (run-level — panel passes are homogeneous; enum `xhigh\|max`, review passes are xhigh-band; omitted = validation error), `justification` (required when `effort` is `max`), `supportedEfforts`, `codexAvailable`. | Workflow tool + subagents; **Codex is runtime-detected, not a dependency** — degrades to single-model on the public mirror. | One-shot fan-out over findings → `no_progress_iterations: null`; ceilings inherit; signal verdict. Cross-harness → Codex tokens at `codex_usd_per_mtok`. | No Codex → every finding judged by Claude alone (`consensus: single-model`). Declared tier ∉ `supportedEfforts` → **fail-closed**: logged note + the whole Codex leg OFF for the run (`consensus: single-model`) — the tier is **never clamped**. `ultra` → hard-rejected. No workflow tool → run the two reviewers per finding sequentially per this row. |
| `drift-holdpr` | **Component-D.** Auto-remediate MECHANICAL drift to a **HOLD PR per project**: one worktree-isolated agent re-syncs every canonical file grouped for that project → verifies → opens a `[HOLD]` PR. Never merges, never touches project main; refuses non-mechanical divergence. | `drifts[]` (`{project,path,canonical,fix}` rows grouped by project; discovered from the latest parent-hook-drift audit if absent), `branchPrefix`. | Workflow tool + subagents + isolated worktrees (parallel PR-opening). | **Opens PRs unattended → declares its OWN ceilings** (not inherited): `max_iterations: 25`, `budget_ceiling_usd: 40`, `no_progress_iterations: null`; signal = HOLD PR opened. | No workflow tool → remediate each project sequentially per this row. No subagents → run inline one project at a time, still HOLD-PR-only. |
| `conductor` | Refresh project refs, rank the fleet backlog against immutable main SHAs, then auto-spec the top eligible item(s) into spec → plan → tasks triads. Honors `auto_spec:true` force ordering, `auto_spec:false` exemptions, and delivered/existing-spec anti-dup exclusions. **Spec-only: never writes implementation code, pushes, or merges.** | `today` (required ISO date), `backlogPath`, `registryPath`, `autoSpecTopN`, optional `weights` (serialized exactly into the rank work order), optional `refreshTimeoutSeconds` (1–300; default 30). | Workflow tool ideal; one-attempt ref-refresh agent plus worktree-isolated subagents for parallel auto-spec fan-out. | One-shot refresh + rank + fan-out → `no_progress_iterations: null`; `budget_ceiling_usd: 60`; `max_iterations` inherits; signal work-list drain. | Any partial/invalid ref refresh aborts before rank. No workflow tool → refresh once, then rank and auto-spec by hand. No subagents → spec selected items sequentially, preserving immutable refs, selection controls, and the spec-only / never-merges boundary. |
| `digest-adopt` | **Implementation half of `research/ai-dev-trends/` (spec 008).** Turn a digest into shipped change: ingest + dedup vs the `adopt-ledger`, skeptically triage each proposal into a route, and — only for human-approved routes — fan out one worktree-isolated agent each through the 006 pipeline to a **HOLD PR**. Propose-only without `args.approved` (the human gate, in code); never merges, never touches project main. | `digestPath` (required), `ledgerPath` (default `research/ai-dev-trends/adopt-ledger.md`), `approved[]` (`{id,route}`; absent → propose-only), `branchPrefix`, caller-resolved `loopSafety` (for canonical `usd_per_mtok` reporting), optional positive `usdPerMTok` per-run override. | Workflow tool + subagents + isolated worktrees (parallel PR-opening). | **Opens PRs unattended → declares its OWN ceilings** (not inherited): `max_iterations: 12`, `budget_ceiling_usd: 60`, `no_progress_iterations: 2`; signal = HOLD PR opened / ledger transition. | No workflow tool → triage then execute each approved item sequentially per this row. No subagents → run inline one item at a time, still propose-gate + HOLD-PR-only. |
| `template` | Blank starting skeleton for authoring a new recipe — `meta` (with the `safety` block), a schema stub, and fan-out/verify scaffolding to fill in. | none (copy + edit). | none — authoring artifact, not runnable as-is. | Carries the `safety` scaffold (inherit-or-override + the null-for-one-shot rule) for the new recipe to fill in. | n/a (scaffold; the recipe you write from it carries its own degrade behavior). |
