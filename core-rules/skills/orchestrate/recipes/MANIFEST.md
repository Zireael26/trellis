# Recipe manifest

One row per recipe. Recipes are generic, parametric skeletons — specifics enter through `args`, never as baked literals. The capability and degrade columns echo the two-level graceful degrade in `SKILL.md`. The **Loop safety** column records each recipe's `meta.safety` declaration — every recipe is a loop and must honor the loop-safety contract (`core-rules/loop-safety.md`); **a recipe with no `safety` declaration is non-compliant** (flagged by `cross-project-process-audit`, a `process-gate` / review finding).

| Recipe | Intent | Inputs (`args`) | Capability needs | Loop safety (`meta.safety`) | Degrade note |
|---|---|---|---|---|---|
| `fanout-verify` | Fan out one verified-change agent per target, push + PR each, return verdicts for the main loop to merge/hold. | `targets[]` (`{name,path}`; falls back to the registry), `task`, `branchPrefix`. | Workflow tool ideal; subagents + isolated worktrees required for parallel fan-out. | One-shot fan-out → `no_progress_iterations: null` (no rounds to measure); `max_iterations` + `budget_ceiling_usd` omitted to inherit the resolved baseline; signal commit/PR. | No workflow tool → dispatch the per-target stage sequentially per this row + `SKILL.md`. No subagents → run targets inline, one at a time, preserving change → verify → verdict. |
| `codex-executor` | Mixed-harness fan-out: route execution-heavy bounded units through the blocking `codex-worker`, keep planning/review/synthesis on the orchestrator, and independently review the actual diff. Contract + dispatch mechanics: [`references/codex-executor.md`](../references/codex-executor.md), intent: `docs/codex-routing.md`. | `units[]` (`{name,kind,task,effort}` + optional `justification`/`paths`/`constraints`/`nonGoals`/`proof`; **`effort` required** on execute units, enum `medium\|high\|xhigh\|max`; omitted = validation error, `ultra` = hard reject, `max` requires non-empty `justification`), `supportedEfforts`, `codexAvailable`, `branchPrefix`. | Workflow tool + inherited `codex-worker`; Codex is runtime-detected, not a dependency. The worker blocks through terminal result; a defensive leaked-handle assertion degrades instead of accepting incomplete work. | One-shot fan-out → `no_progress_iterations: null`; ceilings inherit; signal commit/PR. Cross-harness rates come from config. | No Codex, unsupported surface tier, UNAVAILABLE / FAILURE, empty/error result, or defensive handle assertion → identical unit runs on Claude, logged and never clamped. The legacy rescue forwarder is interactive-rescue-only. No workflow/subagents → preserve route → execute → actual-diff review sequentially. |
| `codex-fanout` | Mixed Codex/Claude implementation fan-out: blocking generation → cheap Claude actual-diff/proof verification → at most one same-leg fix + fresh reverify; returns merge-ready receipts in dependency order and never commits or merges. | `units[]` (`{name,leg:'codex'\|'claude',task,paths,proofCmd,conflicts?,dependsOn?}` plus **required Codex `effort`** enum `medium\|high\|xhigh\|max` and optional `justification`; omitted/unknown throws, `ultra` hard-rejects, `max` requires justification), `codexCap` (required caller-resolved positive integer from `trellis.config.json.codex_fanout.concurrency`, central default 4; never baked into recipe logic), `codexAvailable`, optional `targetCwd` / `companionPath`. | Workflow tool + subagents + inherited `codex-worker`; Claude units consume no Codex slot. `isolation:'worktree'` is set only when `unit.conflicts`. | `max_iterations: null` and `budget_ceiling_usd: null` inherit; `no_progress_iterations: 1` because one worker stall/retry or one repair is the only no-progress allowance; signal file delta. | Codex unavailable / UNAVAILABLE / FAILURE / empty/error → identical unit runs on Claude with `claude(degraded)` receipt. No workflow/subagents → reproduce bounded waves sequentially, preserve independent verification, one-fix cap, and dependency receipt order. Orchestrator alone commits/merges. |
| `verify-panel` | Cross-model diversity verification: per hard finding, run a Claude reviewer + a Codex reviewer in parallel, merge into a consensus (`agree-real` / `agree-not-real` / `split` / `single-model`), degrade to Claude-only when Codex is absent. Contract: [`references/verify-panel.md`](../references/verify-panel.md), intent: `docs/codex-routing.md` §2. | `findings[]` (`{id,claim,file,line,severity}`), `context` (evidence), **`effort` (required**, run-level — panel passes are homogeneous; enum `medium\|high\|xhigh\|max` per `docs/codex-routing.md §3`, review passes are xhigh-band; omitted = validation error, never a default), `justification` (required when `effort` is `max`), `supportedEfforts` (threaded from the D6 preflight; absent → conservative `['medium','high','xhigh']`), `codexAvailable` (threaded; probed if absent). | Workflow tool + subagents; **Codex is runtime-detected, not a dependency** — degrades to single-model on the public mirror. | One-shot fan-out over findings → `no_progress_iterations: null`; ceilings inherit; signal verdict. Cross-harness → Codex tokens at `codex_usd_per_mtok`. | No Codex → every finding judged by Claude alone (`consensus: single-model`). Declared tier ∉ `supportedEfforts` → **fail-closed**: logged note + the whole Codex leg OFF for the run (`consensus: single-model`) — the tier is **never clamped** (spec 011 D6b). `ultra` → hard-rejected (throw + logged note, D4a). No workflow tool → run the two reviewers per finding sequentially per this row. No subagents → judge inline, preserving the independent two-model verdict. |
| `drift-holdpr` | **Component-D.** Auto-remediate MECHANICAL drift (parent-hook-drift / cross-project-process-audit findings) to a **HOLD PR per project**: one worktree-isolated agent re-syncs the canonical file → verifies → opens a `[HOLD]` PR. Never merges, never touches project main; refuses non-mechanical divergence. The new part vs `sync-hooks.sh` is the HOLD-PR orchestration. | `drifts[]` (`{project,path,canonical,fix}`; discovered from the latest parent-hook-drift audit if absent), `branchPrefix`. | Workflow tool + subagents + isolated worktrees (parallel PR-opening). | **Opens PRs unattended → declares its OWN ceilings** (not inherited): `max_iterations: 25`, `budget_ceiling_usd: 40`, `no_progress_iterations: null`; signal = HOLD PR opened. | No workflow tool → remediate each project sequentially per this row. No subagents → run inline one project at a time, still HOLD-PR-only. |
| `template` | Blank starting skeleton for authoring a new recipe — `meta` (with the `safety` block), a schema stub, and fan-out/verify scaffolding to fill in. | none (copy + edit). | none — authoring artifact, not runnable as-is. | Carries the `safety` scaffold (inherit-or-override + the null-for-one-shot rule) for the new recipe to fill in. | n/a (scaffold; the recipe you write from it carries its own degrade behavior). |
