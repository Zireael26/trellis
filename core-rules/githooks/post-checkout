#!/usr/bin/env bash
# Compatibility hook only. New attachments install a clone-local dispatcher
# under TRELLIS_HOME/state/git-hooks/<checkout-id>; that dispatcher preserves
# any pre-existing hook manager and invokes immutable-release reconciliation.
#
# This tracked legacy hook deliberately does nothing. It must remain harmless
# for non-users and never abort Git.
exit 0
