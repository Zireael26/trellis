diff --git a/dist/worktree.js b/dist/worktree.js
index 48d75a7..6d91e2f 100644
--- a/dist/worktree.js
+++ b/dist/worktree.js
@@ -71,8 +71,10 @@ export async function createWorktree(pi, cwd, agentId) {
     const suffix = randomUUID().slice(0, 8);
     const worktreePath = join(tmpdir(), `pi-agent-${agentId}-${suffix}`);
     try {
-        // Create detached worktree at HEAD
-        await git(pi, cwd, ["worktree", "add", "--detach", worktreePath, "HEAD"], 30000);
+        // Create detached worktree at HEAD. 180s, not 30s: a repo's post-checkout
+        // hook can run managed attachment on the new worktree — measured at 95.6s —
+        // and killing git mid-attachment leaves the child running behind our back.
+        await git(pi, cwd, ["worktree", "add", "--detach", worktreePath, "HEAD"], 180000);
         return { path: worktreePath, branch, baseSha, workPath: subdir ? join(worktreePath, subdir) : worktreePath };
     }
     catch {
diff --git a/src/worktree.ts b/src/worktree.ts
index abaec39..0ccbd60 100644
--- a/src/worktree.ts
+++ b/src/worktree.ts
@@ -108,8 +108,10 @@ export async function createWorktree(
   const worktreePath = join(tmpdir(), `pi-agent-${agentId}-${suffix}`);
 
   try {
-    // Create detached worktree at HEAD
-    await git(pi, cwd, ["worktree", "add", "--detach", worktreePath, "HEAD"], 30000);
+    // Create detached worktree at HEAD. 180s, not 30s: a repo's post-checkout
+    // hook can run managed attachment on the new worktree — measured at 95.6s —
+    // and killing git mid-attachment leaves the child running behind our back.
+    await git(pi, cwd, ["worktree", "add", "--detach", worktreePath, "HEAD"], 180000);
     return { path: worktreePath, branch, baseSha, workPath: subdir ? join(worktreePath, subdir) : worktreePath };
   } catch {
     // If worktree creation fails, return undefined (agent runs in normal cwd)
